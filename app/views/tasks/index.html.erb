
<!DOCTYPE html>
<html>
  <head>
    <title>My Tasks</title>
  </head>

  <body>
  <div class="container">
    <h1>My Tasks</h1>
        <ul>
        <% @tasks.each do |task| %>
            <li><%= link_to task.title, task_path(task) %>
            | <%= link_to "", task_path(task), method: :delete, data: { confirm: "Are you sure?" }, class: "fa fa-trash" %>
<!--        | <%= link_to "Edit", edit_task_path(task), class: "btn btn-primary" %>  Not needed in this challenge since in individual tabs-->
            </li>
        <% end %>
        </ul>
    <p class="fas fa-plus", id="plus"><%= link_to "Create a new task", new_task_path %></p>
    </body>
  </div>
</html>
